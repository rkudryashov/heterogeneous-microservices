micronaut:
  application:
    name: micronaut-service
  server:
    port: 8084

consul:
  client:
    registration:
      enabled: true

application-info:
  name: ${micronaut.application.name}
  framework:
    name: Micronaut
    releaseYear: 2018